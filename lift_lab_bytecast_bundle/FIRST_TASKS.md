# First Tasks (Lift Lab Bundle)

Use the bundle-local starter board:
- `docs/FIRST_TASKS.md`

Top quick wins:
1. Add one slide in `content/slides.json`.
2. Add one mission in `content/missions.json`.
3. Improve wording in `site/index.html`.
