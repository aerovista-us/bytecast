# Copy to `.env` in this directory (Compose loads it automatically).
# Uncomment one line when host port 8080 is already in use.
#
# On nxcore, 8080 is usually av-catalog-console — use a dedicated port, e.g.:
# BYTECAST_PORT=18080
#
# Generic alternate:
# BYTECAST_PORT=8081
